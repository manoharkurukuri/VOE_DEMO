[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q
markers =
    integration: live tests that hit real services (Playwright browser, real dealer page, Gemini LLM). Deselect with '-m "not integration"'.
